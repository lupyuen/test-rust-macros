# Rust build settings
[build]

# Rust compiler options
rustflags = [
    # Allow unstable (experimental) options like `--pretty`
    # 

    # Expand macros
    # "-Z", "unstable-options", "--pretty",  "expanded",

    # Expand macros and show hygiene info
    "-Z", "unstable-options", "--pretty",  "expanded,hygiene",
]